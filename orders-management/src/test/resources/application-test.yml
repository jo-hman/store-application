eureka:
    client:
      enabled: false
spring:
  rabbitmq:
    host: localhost
    username: guest
    password: guest
    virtual-host: /
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
  application:
    name: orders-management
  webclient:
    product:
      base-url: http://localhost:8082
